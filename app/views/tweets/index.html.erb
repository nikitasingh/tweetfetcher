<div class="page" id="" >
<div class = "tweets">
  <%= render partial: "tweet" %>
</div>
	<center>
		<div class = "ajax_loader" >
			<%= image_tag "loader.gif" %>
		</div>
	</center>
</div>

<%= hidden_field_tag :last_id, @tweets.last.id %>
<%= hidden_field_tag :last_tweet_date, @tweets.last.created_at.strftime("%d-%b-%Y") %>
